class Solution {
    public boolean search(int[] nums, int target) {
        boolean output=false;
        for(int i=0;i<nums.length;i++){
            if(nums[i]==target){
                output=true;
                break;
            }else {
                output= false;
            }
        }
        return output;
    }
}
