class Solution {
    public String reverseWords(String s) {
        s=s.trim();
        String output="";
       String[] s1=s.split("\\s+"); 
       for(int i=s1.length-1;i>=0;i--){
          output+=s1[i]+" ";
       }
       return output.trim();
    }
}
