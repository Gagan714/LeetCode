class Solution {
    public int minPartitions(String n) {
        int output=0;
        for(int i=0;i<n.length();i++){
            int x=n.charAt(i)-'0';
            output=Math.max(output,x);
        }
        return output;
    }
}
